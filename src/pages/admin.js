// src/pages/admin.js
"use client";

export default function AdminLoginPage({ user }) {
  // AdminAuthWrapper (in _app.js) renders the Authenticator UI.
  // We don't need to render anything of our own here.
  return null;
}